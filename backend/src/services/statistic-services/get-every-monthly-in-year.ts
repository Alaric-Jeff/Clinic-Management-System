import type { FastifyInstance } from "fastify";
// should I use date-fns
export async function getEveryMonthInYearService(
    fastify: FastifyInstance
){
    try{

    }catch(err: unknown){
        throw err;
    }
}